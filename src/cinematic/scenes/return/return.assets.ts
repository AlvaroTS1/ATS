import type { SceneAssets } from '../../types';

/** Return is fully procedural (particle collapse + glow) — nothing to fetch. */
export const RETURN_ASSETS: SceneAssets = {
  id: 'return',
  preloadPriority: 6,
};
