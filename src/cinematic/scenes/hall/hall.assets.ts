import type { SceneAssets } from '../../types';

/** The Hall is fully procedural (particles organizing into a room + ambient glow) — nothing to fetch. */
export const HALL_ASSETS: SceneAssets = {
  id: 'hall',
  preloadPriority: 5,
};
